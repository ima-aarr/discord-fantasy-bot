discord.py==2.3.1

# CPU 版 PyTorch を公式インデックスから直接
torch==2.9.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu

# transformers は tokenizers の prebuilt wheel があるバージョン
transformers==4.41.0
tokenizers==0.19.1

# その他依存
aiohttp<4,>=3.7.4
